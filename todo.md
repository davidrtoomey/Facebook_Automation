## OFFER AGENT
1. ~~fix the bug where it keeps clicking the location underneath the send button instead of just clicking the send~~ FIXED - Now uses Enter key to send instead of clicking
2. ~~Sometimes it clears out the default message "Hi, is this still available?" sometimes it does not (before it sends the offer)~~ FIXED - Now uses input action with clear=True
